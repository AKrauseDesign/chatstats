module.exports = function(client, db, request) {
  // if(message.toLowerCase().indexOf('!lookup') === 0) {
  //   if(user.username === 'inormous' || user.username === 'massansc' || user.username === 'stylerdev') {
  //     request('http://api.newtimenow.com/follow-length/?channel=massansc&user='+split[1], function (error, response, body) {
  //       if (!error && response.statusCode === 200) {
  //         Users.findOne({ where: {name: split[1]} }).then(function(lookupUser) {
  //           client.say(channel,  split[1] + ' Has watched: '+ minuteToHour(lookupUser.watchedTime) + ' Hours, and written ' + lookupUser.count + ' Lines of text - Followed since: '+body);
  //         });
  //       }
  //     });
  //   }
  // }
};
